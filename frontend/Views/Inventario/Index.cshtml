@model IEnumerable<frontend.Models.Producto>

<div class="card shadow">
    <div class="card-header bg-white d-flex justify-content-between align-items-center">
        <h4 class="mb-0 text-primary">Gestión de Inventario</h4>
        <a href="/Inventario/Formulario" class="btn btn-primary">Agregar Producto</a>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>ID</th>
                        <th>Nombre</th>
                        <th>Descripción</th>
                        <th>Categoría</th>
                        <th>Precio</th>
                        <th>Stock</th>
                        <th>Vencimiento</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.Id</td>
                                <td><strong>@item.Nombre</strong></td>
                                <td>@item.Descripcion</td>
                                <td><span>@item.Categoria</span></td>
                                <td>Q @(item.Precio.ToString("F2"))</td>
                                <td>
                                    <span>@item.CantidadStock</span>
                                </td>
                                <td>@(item.FechaVencimiento?.ToString("dd/MM/yyyy") ?? "N/A")</td>
                                <td>
                                    <div class="btn-group">
                                        <a href="/Inventario/Modificar/@item.Id" class="btn btn-warning btn-sm">Modificar</a>
                                        <form action="/Inventario/Eliminar" method="post" style="display:inline;" onsubmit="return confirm('¿Estás seguro de que deseas eliminar este producto?');">
                                            <input type="hidden" name="id" value="@item.Id" />
                                            <button type="submit" class="btn btn-danger btn-sm">Eliminar</button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="8" class="text-center text-muted">No hay productos cargados aun...</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>